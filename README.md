# Azure Data Factory Incremental Pipeline with Schema Evolution

## 📌 Project Overview

This project demonstrates an **end-to-end data engineering pipeline** built using **Azure Data Factory (ADF)**, **PySpark**, and **Power BI**.

The pipeline ingests **Excel source files with evolving schemas**, processes data **incrementally**, and delivers **analytics-ready curated data** for reporting.  
It is designed following **production-grade patterns** commonly used in enterprise data platforms.

---

## 🎯 Key Objectives

- Ingest Excel files with **changing schemas**
- Handle **schema drift** without breaking pipelines
- Implement **incremental loading** using a watermark strategy
- Perform transformations using **PySpark**
- Orchestrate the workflow using **Azure Data Factory**
- Enable efficient reporting using **Power BI incremental refresh**

---

## 🏗️ Architecture

